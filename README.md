# README

Work in progress... 

Instructions:

`bundle install`
`rake db:create`
`rake db:migrate`

Routes:

Create a user:
'POST /users'

Get Token from user
`GET users/:id/connect?public_token='GET PUBLIC TOKEN FROM LINK'`
